# AI independence policy

Transport identity is not model independence.

Future backend descriptors should carry provider, model, model_family, transport, and independence_group. HIVE School must never treat teacher and verifier as independent solely because their transport or route IDs differ.
