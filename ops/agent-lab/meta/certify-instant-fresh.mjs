import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';

const CDP=process.env.META_LAB_CDP_URL||'http://127.0.0.1:9232';
const OUT_DIR=process.env.META_LAB_CERT_DIR||path.resolve('certifications');
const TEST_ID='HIVE-META-STRESS-0001-FRESH';
const nonce=crypto.randomBytes(4).toString('hex').toUpperCase();
const MASTER={salt:7919,coeff:[17,29,43]};
const SHARDS={A:[311,7,19,23,5],B:[41,53,67,71,73],C:[79,83,89,97,101]};
const SUMS={A:SHARDS.A.reduce((a,b)=>a+b,0),B:SHARDS.B.reduce((a,b)=>a+b,0),C:SHARDS.C.reduce((a,b)=>a+b,0)};
const TERMS={A:MASTER.coeff[0]*SUMS.A,B:MASTER.coeff[1]*SUMS.B,C:MASTER.coeff[2]*SUMS.C};
const EXPECTED=MASTER.salt+TERMS.A+TERMS.B+TERMS.C;
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
const log=(...x)=>console.log(new Date().toISOString(),...x);

async function json(url,opts={}){const r=await fetch(url,opts);if(!r.ok)throw new Error(`HTTP_${r.status}_${url}`);return r.json();}
const targets=await json(`${CDP}/json/list`);
const target=targets.find(t=>t.type==='page'&&/meta\.ai/i.test(t.url||''));
if(!target?.webSocketDebuggerUrl)throw new Error('META_PAGE_NOT_FOUND');
const ws=new WebSocket(target.webSocketDebuggerUrl);
await new Promise((resolve,reject)=>{const timer=setTimeout(()=>reject(new Error('PAGE_CDP_CONNECT_TIMEOUT')),15000);ws.addEventListener('open',()=>{clearTimeout(timer);resolve();},{once:true});ws.addEventListener('error',()=>{clearTimeout(timer);reject(new Error('PAGE_CDP_CONNECT_ERROR'));},{once:true});});
let seq=0;const pending=new Map();
ws.addEventListener('message',ev=>{let m;try{m=JSON.parse(ev.data);}catch{return;}if(!m.id||!pending.has(m.id))return;const p=pending.get(m.id);pending.delete(m.id);clearTimeout(p.timer);m.error?p.reject(new Error(`CDP_${m.error.code}:${m.error.message}`)):p.resolve(m.result||{});});
function call(method,params={}){const id=++seq;return new Promise((resolve,reject)=>{const timer=setTimeout(()=>{pending.delete(id);reject(new Error(`CDP_TIMEOUT_${method}`));},30000);pending.set(id,{resolve,reject,timer});ws.send(JSON.stringify({id,method,params}));});}
async function evalJs(expression){const r=await call('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true});if(r.exceptionDetails)throw new Error(`EVAL_EXCEPTION_${r.exceptionDetails.text||'unknown'}`);return r.result?.value;}
await call('Runtime.enable');await call('Page.enable').catch(()=>{});

async function bodyText(){return evalJs(`String(document.body?.innerText||'')`).catch(()=> '');}
function count(text,token){return token?text.split(token).length-1:0;}
async function uiState(){return evalJs(`(()=>{const visible=e=>{const r=e.getBoundingClientRect(),s=getComputedStyle(e);return r.width>0&&r.height>0&&s.display!=='none'&&s.visibility!=='hidden'};const all=[...document.querySelectorAll('input,textarea,[contenteditable="true"],[role="textbox"]')].filter(visible);const composer=all.find(e=>/ask meta ai/i.test(String(e.getAttribute('aria-label')||e.getAttribute('placeholder')||'')))||all.find(e=>e.getAttribute('contenteditable')==='true'&&e.getAttribute('role')==='textbox')||all.find(e=>e.getAttribute('contenteditable')==='true')||null;const controls=[...document.querySelectorAll('button,a,[role="button"]')].filter(visible);const send=controls.find(e=>/^send$/i.test(String(e.getAttribute('aria-label')||e.innerText||'').trim()));const stop=controls.find(e=>/^(stop|stop generating|stop responding|cancel response)$/i.test(String(e.getAttribute('aria-label')||e.innerText||'').trim()));const login=controls.find(e=>/^log in$/i.test(String(e.innerText||e.getAttribute('aria-label')||'').trim()));const instant=controls.find(e=>/^instant$/i.test(String(e.innerText||e.getAttribute('aria-label')||'').trim()));const text=composer?String(composer.value||composer.innerText||composer.textContent||''):'';return {href:String(location.href||''),composer:!!composer,composerText:text,composerTag:composer?.tagName?.toLowerCase()||null,sendVisible:!!send,sendDisabled:send?!!send.disabled:null,stopVisible:!!stop,loginVisible:!!login,instantVisible:!!instant,instantPressed:instant?.getAttribute('aria-pressed')??null};})()`);}
async function waitComposer(timeout=45000){const s=Date.now();while(Date.now()-s<timeout){const u=await uiState();if(u.composer)return u;await sleep(400);}throw new Error('META_COMPOSER_NOT_READY');}
async function waitIdle(timeout=60000){const s=Date.now();let stable=0;while(Date.now()-s<timeout){const u=await uiState();if(u.composer&&!u.stopVisible){stable++;if(stable>=4)return true;}else stable=0;await sleep(350);}return false;}
async function focusComposer(){return evalJs(`(()=>{const visible=e=>{const r=e.getBoundingClientRect(),s=getComputedStyle(e);return r.width>0&&r.height>0&&s.display!=='none'&&s.visibility!=='hidden'};const all=[...document.querySelectorAll('input,textarea,[contenteditable="true"],[role="textbox"]')].filter(visible);const e=all.find(x=>/ask meta ai/i.test(String(x.getAttribute('aria-label')||x.getAttribute('placeholder')||'')))||all.find(x=>x.getAttribute('contenteditable')==='true'&&x.getAttribute('role')==='textbox')||all.find(x=>x.getAttribute('contenteditable')==='true')||null;if(!e)return false;e.focus();return true;})()`);}
async function clearComposer(){if(!await focusComposer())throw new Error('META_COMPOSER_NOT_FOUND');await call('Input.dispatchKeyEvent',{type:'rawKeyDown',modifiers:2,key:'a',code:'KeyA',windowsVirtualKeyCode:65});await call('Input.dispatchKeyEvent',{type:'keyUp',modifiers:2,key:'a',code:'KeyA',windowsVirtualKeyCode:65});await call('Input.dispatchKeyEvent',{type:'rawKeyDown',key:'Backspace',code:'Backspace',windowsVirtualKeyCode:8});await call('Input.dispatchKeyEvent',{type:'keyUp',key:'Backspace',code:'Backspace',windowsVirtualKeyCode:8});await sleep(250);}
async function clickSend(){return evalJs(`(()=>{const visible=e=>{const r=e.getBoundingClientRect(),s=getComputedStyle(e);return r.width>0&&r.height>0&&s.display!=='none'&&s.visibility!=='hidden'};const e=[...document.querySelectorAll('button,[role="button"]')].filter(visible).find(x=>/^send$/i.test(String(x.getAttribute('aria-label')||x.innerText||'').trim()));if(!e||e.disabled)return false;e.click();return true;})()`);}
async function pressEnter(){if(!await focusComposer())return false;await call('Input.dispatchKeyEvent',{type:'rawKeyDown',key:'Enter',code:'Enter',windowsVirtualKeyCode:13});await call('Input.dispatchKeyEvent',{type:'keyUp',key:'Enter',code:'Enter',windowsVirtualKeyCode:13});return true;}
async function verifySubmitted(token,timeout=12000){const s=Date.now();while(Date.now()-s<timeout){const u=await uiState(),b=await bodyText();if(!u.composerText.includes(nonce)&&count(b,token)>=1)return true;await sleep(300);}return false;}
async function send(text,token,label){if(!await waitIdle())throw new Error(`META_NOT_IDLE_BEFORE_${label}`);await clearComposer();await call('Input.insertText',{text});await sleep(450);let u=await uiState();if(!u.composerText.includes(nonce))throw new Error(`META_TEXT_NOT_INSERTED_${label}`);let method='SEND_BUTTON';if(!(u.sendVisible&&!u.sendDisabled&&await clickSend())){method='ENTER';await pressEnter();}log('SUBMIT_ATTEMPT',label,method);if(!await verifySubmitted(token))throw new Error(`META_PROMPT_NOT_SUBMITTED_${label}`);log('SUBMITTED',label,method,'composer-cleared+prompt-visible');return method;}
async function waitExact(expected,before,timeout=180000){const s=Date.now();let last=-1,stable=0;while(Date.now()-s<timeout){const b=await bodyText();const n=count(b,expected);if(n>=before+2){if(n===last)stable++;else stable=0;last=n;if(stable>=2&&await waitIdle(30000))return 'body-occurrence-exact+stable';}await sleep(750);}throw new Error(`TIMEOUT_WAITING_META_EXACT_${expected}`);}
async function stage(label,prompt,echo){const before=count(await bodyText(),echo);log('SEND',label);await send(prompt,echo,label);const v=await waitExact(echo,before);log('PASS',label,v,'exact-data-echo');}
function finalMatches(text){const re=new RegExp(`META_CERT_RESULT:${nonce}:(\\d+):(\\d+):(\\d+):(\\d+):(\\d+):(\\d+):(\\d+):(\\d+):(\\d+):(\\d+):(\\d+)`,'g');return [...text.matchAll(re)];}
function parseFinal(m){return {salt:Number(m[1]),coeff:[Number(m[2]),Number(m[3]),Number(m[4])],sums:{A:Number(m[5]),B:Number(m[6]),C:Number(m[7])},terms:{A:Number(m[8]),B:Number(m[9]),C:Number(m[10])},checksum:Number(m[11])};}
async function waitFinal(timeout=240000){const s=Date.now();let raw='',stable=0;while(Date.now()-s<timeout){const matches=finalMatches(await bodyText());if(matches.length){const m=matches.at(-1);if(m[0]===raw)stable++;else{raw=m[0];stable=1;}if(stable>=4&&await waitIdle(30000)){await sleep(1500);const m2=finalMatches(await bodyText()).at(-1);if(m2&&m2[0]===raw)return parseFinal(m2);}}await sleep(900);}throw new Error(`TIMEOUT_WAITING_STABLE_META_FINAL_${nonce}`);}
async function freshChat(){const clicked=await evalJs(`(()=>{const visible=e=>{const r=e.getBoundingClientRect(),s=getComputedStyle(e);return r.width>0&&r.height>0&&s.display!=='none'&&s.visibility!=='hidden'};const els=[...document.querySelectorAll('a,button,[role="button"]')].filter(visible);const e=els.find(x=>/^new chat(?:\\s|$)/i.test(String(x.innerText||x.getAttribute('aria-label')||'').trim()));if(!e)return false;e.click();return true;})()`);if(!clicked)throw new Error('META_NEW_CHAT_CONTROL_NOT_FOUND');await sleep(1200);await waitComposer();console.log('FRESH_CHAT_READY=true');console.log(`FRESH_CHAT_URL=${(await uiState()).href}`);}

async function main(){fs.mkdirSync(OUT_DIR,{recursive:true});await waitComposer();let state=await uiState();if(state.loginVisible)throw new Error('META_LOGIN_REQUIRED');await freshChat();state=await uiState();if(state.loginVisible)throw new Error('META_LOGIN_REQUIRED_AFTER_NEW_CHAT');console.log('=== META AI CERTIFICATION ===');console.log(`TEST_ID=${TEST_ID}`);console.log('BENCHMARK_UNCHANGED=true');log(`START ${TEST_ID} nonce=${nonce} expected=${EXPECTED} sums=${SUMS.A},${SUMS.B},${SUMS.C} terms=${TERMS.A},${TERMS.B},${TERMS.C}`);log(`MODE InstantVisible=${state.instantVisible} authenticated=${!state.loginVisible} composer=${state.composer}`);
const masterEcho=`ACK_MASTER:${nonce}:${MASTER.salt}:${MASTER.coeff.join(',')}`;await stage('MASTER',[`${TEST_ID} / ${nonce}`,'Memorizza esattamente questo MASTER per un test multi-turn. Non calcolare ancora il risultato finale.',`SALT=${MASTER.salt}`,`COEFF_A=${MASTER.coeff[0]}`,`COEFF_B=${MASTER.coeff[1]}`,`COEFF_C=${MASTER.coeff[2]}`,`Rispondi ESATTAMENTE: ${masterEcho}`].join('\n'),masterEcho);
const echoA=`ACK_A:${nonce}:${SHARDS.A.join(',')}`;await stage('SHARD_A',[`${TEST_ID} / ${nonce}`,`SHARD_A=${SHARDS.A.join(',')}`,`Memorizzalo esattamente e rispondi ESATTAMENTE: ${echoA}`].join('\n'),echoA);
const echoB=`ACK_B:${nonce}:${SHARDS.B.join(',')}`;await stage('SHARD_B',[`${TEST_ID} / ${nonce}`,`SHARD_B=${SHARDS.B.join(',')}`,`Memorizzalo esattamente e rispondi ESATTAMENTE: ${echoB}`].join('\n'),echoB);
const echoC=`ACK_C:${nonce}:${SHARDS.C.join(',')}`;await stage('SHARD_C',[`${TEST_ID} / ${nonce}`,`SHARD_C=${SHARDS.C.join(',')}`,`Memorizzalo esattamente e rispondi ESATTAMENTE: ${echoC}`].join('\n'),echoC);
log('SEND FINAL');const finalPrompt=[`${TEST_ID} / ${nonce}`,'Usa SOLO il MASTER e i tre shard memorizzati nei messaggi precedenti.','Calcola SUM_A, SUM_B, SUM_C. Poi TERM_A=COEFF_A*SUM_A, TERM_B=COEFF_B*SUM_B, TERM_C=COEFF_C*SUM_C. Infine CHECKSUM=SALT+TERM_A+TERM_B+TERM_C.','Non aggiungere spiegazioni.',`Rispondi ESATTAMENTE nel formato: META_CERT_RESULT:${nonce}:<SALT>:<COEFF_A>:<COEFF_B>:<COEFF_C>:<SUM_A>:<SUM_B>:<SUM_C>:<TERM_A>:<TERM_B>:<TERM_C>:<CHECKSUM>`].join('\n');await send(finalPrompt,`META_CERT_RESULT:${nonce}:`,'FINAL');const final=await waitFinal();const passed=final.salt===MASTER.salt&&final.coeff[0]===17&&final.coeff[1]===29&&final.coeff[2]===43&&final.sums.A===SUMS.A&&final.sums.B===SUMS.B&&final.sums.C===SUMS.C&&final.terms.A===TERMS.A&&final.terms.B===TERMS.B&&final.terms.C===TERMS.C&&final.checksum===EXPECTED;const cert={test_id:TEST_ID,nonce,provider:'Meta AI',product:'Meta AI Web',transport:'browser-session-direct-cdp',mode:state.instantVisible?'Instant':'UI mode not confirmed',authenticated:!state.loginVisible,fresh_chat:true,streaming_stability_guard:true,api_required:false,zero_cost_path:true,expected:{master:MASTER,sums:SUMS,terms:TERMS,checksum:EXPECTED},actual:final,certified:passed};const file=path.join(OUT_DIR,`${TEST_ID}-${nonce}.json`);fs.writeFileSync(file,JSON.stringify(cert,null,2));console.log('');console.log(`META_CERTIFIED=${passed?'true':'false'}`);console.log(`TEST_ID=${TEST_ID}`);console.log(`NONCE=${nonce}`);console.log('PRODUCT=Meta AI Web');console.log(`MODE=${state.instantVisible?'Instant':'not-confirmed'}`);console.log(`AUTHENTICATED=${!state.loginVisible?'true':'false'}`);console.log('FRESH_CHAT=true');console.log('STREAMING_STABILITY_GUARD=true');console.log(`EXPECTED_MASTER=${MASTER.salt}:${MASTER.coeff.join(',')}`);console.log(`ACTUAL_MASTER=${final.salt}:${final.coeff.join(',')}`);console.log(`EXPECTED_SUMS=${SUMS.A},${SUMS.B},${SUMS.C}`);console.log(`ACTUAL_SUMS=${final.sums.A},${final.sums.B},${final.sums.C}`);console.log(`EXPECTED_TERMS=${TERMS.A},${TERMS.B},${TERMS.C}`);console.log(`ACTUAL_TERMS=${final.terms.A},${final.terms.B},${final.terms.C}`);console.log(`EXPECTED_CHECKSUM=${EXPECTED}`);console.log(`ACTUAL_CHECKSUM=${final.checksum}`);console.log(`CERTIFICATE=${file}`);ws.close();if(!passed)process.exit(2);}
main().catch(err=>{console.error('META_CERTIFIED=false');console.error(`ERROR=${err.message}`);try{ws.close();}catch{}process.exit(1);});
