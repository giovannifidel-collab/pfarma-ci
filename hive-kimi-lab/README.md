# HIVE ↔ Kimi Roundtrip Lab

This directory is a disposable integration surface for proving the HIVE collective-intelligence collaboration loop without touching any production project.

Rules:

- Work only on an isolated branch based on `hive-cloud-computer-v0`.
- Never modify production project pointers, secrets, existing workflows, or canonical branches.
- The first task is `HIVE-KIMI-0001`.
- The expected output is a new JSON result envelope only.
- The pull request must target `hive-cloud-computer-v0`, never `main`.
- GitHub Actions will validate the result automatically.
- Independent GPT/HIVE review happens after CI.
