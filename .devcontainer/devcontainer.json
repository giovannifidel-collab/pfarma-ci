{
  "name": "HIVE Kimi Cloud Bootstrap",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "bash hive-kimi-cloud/install.sh",
  "postStartCommand": "bash -lc 'mkdir -p \"$HOME/.hive-agent-lab\"; if ! pgrep -f \"ops/agent-lab/bridge/auto-heal-queen.sh\" >/dev/null 2>&1; then cd /workspaces/pfarma-ci && nohup bash ops/agent-lab/bridge/auto-heal-queen.sh >\"$HOME/.hive-agent-lab/auto-heal.log\" 2>&1 & fi'",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  }
}
